import { Toolbar as ToolbarRoot, ToolbarGroup, ToolbarSeparator } from '@/components/ui/toolbar'

const Toolbar = Object.assign(ToolbarRoot, {
  Group: ToolbarGroup,
  Separator: ToolbarSeparator,
})

export { Toolbar }
