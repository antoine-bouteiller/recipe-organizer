# 09 — Miscellaneous cleanup

Smaller items that don't belong in any of the topical specs but still need
attention before the migration is done.

## PWA / Serwist

- `scripts/generate-sw.ts` (`tanstackSerwistPlugin`) — keep. It's a plain
  Vite plugin and only depends on `@serwist/build` / `serwist`. Verify
  during implementation that:
  - The plugin still picks up the new `dist/` shape produced by `voidPlugin()`
  - The `precacheManifest` covers `pages/` build output rather than
    `routes/` (TanStack file-based) output
  - If the plugin assumes specific output paths from TanStack Start, patch
    it to match Void's output
- `src/sw.ts` — keep, this is the service worker entry. Confirm it still
  registers cleanly from the new `pages/layout.tsx`.
- Service worker registration code currently lives in `__root.tsx`'s
  `useEffect`. Extract to `src/components/service-worker.tsx`
  (client component) and render it inside `pages/layout.tsx`.
- `public/manifest.json` — keep as-is.

## Devtools

- `@tanstack/react-devtools`, `@tanstack/devtools-vite`,
  `@tanstack/react-router-devtools`, `@tanstack/react-query-devtools` —
  all removed.
- Void has no equivalent "in-app devtools panel"; if the team relied on
  these for debugging, the substitute is browser devtools + `void project
logs`. Network tab will show Inertia-style JSON props on navigation.

## State stores (Zustand)

- `src/stores/recipe-quantities.store.ts`
- `src/stores/shopping-list.store.ts`

Both are pure client-state stores backed by Zustand + (likely) localStorage
persistence. They're framework-agnostic and survive the migration unchanged.
**Action: none.**

## Generated artifacts

- `src/routeTree.gen.ts` — DELETE. No TanStack Router → no route tree.
- `.tanstack/` directory — DELETE (TanStack's local cache).
- `.wrangler/` directory — DELETE if direct wrangler deploy is not used.
- `worker-configuration.d.ts` — DELETE (was generated by `wrangler types`).
- `database.sql` — DELETE or move outside the repo (was an emergency
  snapshot used by `db:dump`/`db:import`).

## Editor / IDE

- `.zed/` — keep as-is (editor settings).
- `.vite-hooks/` — keep (Vite+ artifact).
- `.void/` — gitignored except `.void/project.json` (see
  [01-toolchain](./01-toolchain-and-config.spec.md#gitignore)).

## Renovate / dependency bots

`renovate.json5` watches the current dependency set. After the migration:

- The removed packages stop receiving updates (good — they're gone).
- The new packages (`void`, `@void/react`) get auto-PRs. Confirm
  `renovate.json5` rules don't lock specific versions in a way that
  conflicts with Void's release cadence.
- The `pnpm.overrides` entry pinning `vite` to
  `npm:@voidzero-dev/vite-plus-core@latest` — Renovate should not try to
  update this (it's an override, not a direct dep). Verify the
  `renovate.json5` ignore list.

## ESLint config

Several `lint.rules` and `lint.overrides` in `vite.config.ts` reference
TanStack/wrangler-specific files (`use-file-upload.ts`, `routeTree.gen.ts`,
`vite.config.ts`). After cleanup:

- `'**/routeTree.gen.ts'` ignore can be removed
- The `use-file-upload.ts` override **stays** if the rewritten file still
  needs the `react-hooks/exhaustive-deps: 'off'` exemption (likely yes)

## `AGENTS.md` / `CLAUDE.md`

The current `AGENTS.md` (mirrored as `CLAUDE.md`) describes the
TanStack/wrangler reality:

- The "Critical Rules" section mentions `pnpm migration:apply:*` —
  replace with `pnpm db:migrate:local` / `pnpm db:migrate:remote`.
- The Void block (already injected as `<!--injected-by-void-v0.7.3-->`)
  stays.
- The Vite+ block stays.
- The "Guidelines" pointer to `docs/infrastructure/*.spec.md` — those
  specs still describe TanStack Start. Either:
  - Update them to describe the new Pages-mode reality (significant work,
    but they're the canonical reference per `CLAUDE.md`)
  - Mark them stale with a banner pointing to `void-migration/` until
    rewritten
- Add a pointer to `void-migration/index.spec.md` for the migration period.

**Recommended:** after the migration lands, do a follow-up PR that
rewrites `docs/infrastructure/*.spec.md` to match the new architecture.
That work is **out of scope** for this migration spec.

## `docs/` directory

Same as `AGENTS.md`: `docs/architecture.spec.md`,
`docs/file-structure.spec.md`, and `docs/infrastructure/*.spec.md` are all
TanStack-centric. Treat as stale post-migration. Plan a documentation
follow-up.

## Per-feature spec docs

Each `src/features/<name>/<name>.spec.md` (e.g.
`src/features/recipe/spec/index.spec.md`) describes the current
TanStack-based implementation. They contain valuable domain logic
descriptions (vegetarian tag inference, magimix detection, etc.) that
survive the migration unchanged — but their **code references** will be
stale.

After migration:

- Update the import paths and API patterns in each feature spec
- Drop sections that describe TanStack-specific concerns
- Keep the domain logic descriptions

This is also a documentation follow-up, out of scope for the migration
itself.

## Verification gate

This phase is "done" when:

- Service worker registers and caches on the deployed site
- No dead files remain (`grep -r 'routeTree\|createFileRoute\|useAppSession'`
  returns nothing in `src/`)
- `.gitignore` is updated
- Renovate config doesn't break post-migration
- `AGENTS.md` is patched to reference the new commands at minimum (the
  deep doc rewrite is a follow-up)
